# B 类补丁纲领 — v0.2.1 偏差修复批次（2026-08-19）

> 定位: K3 纲领（`K3_GUIDANCE_2026-08-18.md`）管辖 A 级**正确性修复**（fix(A\*) 系列，
> A1-A5 已全部落地: PR #16/#18/#19/#20/#21）。本纲领管辖 B 级**补丁修改**
> （patch(B\*) 系列）——对 v0.2.1 时代「部分训练未按计划完成、部分数据
> 算完即提交未经处理」造成的计划偏差与文档失真，逐项打补丁。
>
> 触发指令（2026-08-19）: v0.2.1 时有部分训练数据没有经过处理，算完之后
> 就提交到仓库里了；对当前后续的优化以及数学公式等的搭建不一定正确；
> 整个工作流程或因当时的部分训练未按计划完成。要求以 A 系列同构的
> 格式出 B 类纲领，B1 = 全项目偏差审计。

---

## 0. 命名与流程规范（与 A 系列同构）

| 项 | 规范 |
|---|---|
| 分支 | `patch/B{n}-{slug}`（自最新 main 切出，一补丁一分支一提交） |
| 提交 | `patch(B{n}): {标题}` —— 正文分节: 问题/补丁/守卫/验收/待办 |
| PR | 标题同提交；描述含问题、补丁表、验收清单、影响面 |
| 验收门槛 | `scripts/self_check.py` 退出码 0 + 全量 pytest 通过且测试数只增不减 + CHANGELOG 同步 |
| 语义边界 | B 类是补丁: 文档标注/状态同步/数据补算/工程债清偿。**不得**改变预注册判据的语义；判据数值锚点若需换算，按 B4 声明后执行 |
| 编号接管 | 本纲领接管 K3 §B 级清单——原 K3 B1-B10 映射为本纲领 B6-B15（见 §2 映射表），K3 原文编号冻结不改 |

---

## 1. B1 — 全项目偏差审计（本纲领即审计产物，2026-08-19 完成）

### 1.1 审计方法

对照 v0.2.1（tag `1f17806`，2026-08-18 CI 批次）时点的计划文档
（`ROADMAP.md` / `PLAN_2026-08-17-transformer-scaleup.md` / `EXPERIMENT_REGISTRY.md`）
与实际产物（`UPDATE_LOG_2026-08-17-transformer.md`、登记表、磁盘 outputs/），
逐项核对「计划 → 实际」差异；再叠加 A1-A5 修复对历史数字的口径冲击。

### 1.2 计划 vs 实际出入清单（v0.2.1 偏差主表）

| # | 计划（v0.2.1 时点） | 实际 | 偏差影响 |
|---|---|---|---|
| D1 | tf100_128x4（波1，特征自注意力 vs MLP） | ⏸ Stage3 OOM 后重启即被暂停，无报告 | 架构升级第一问悬空 |
| D2 | tf300_128x4（波1，规模鲁棒性） | ⏸ Stage1 中途暂停，无报告 | MLP@300 崩溃是否架构性问题无证据 |
| D3 | tf100_256x8（波2，容量扫描） | ⏳ 未开始 | 容量增益无数据 |
| D4 | PLAN §3 完成后判定路径（推翻/维持 NO-GO） | 悬空——判定停在 NO-GO 但架构升级线未走完 | 判定书对 transformer 线未收口 |
| D5 | PLAN §4 P0-P3 后续架构候选 | 全部未做且未标注状态 | 计划漂移未声明 |
| D6 | 实验数据「算完 → 复核 → 登记」 | 78 份 EXP json 与登记表数字算完即写入，未经独立复核 | 下条 |
| D7 | 登记数字作为后续优化/公式搭建的参照系 | 全部产出自 A1-A5 修复前的训练/评估代码 | **参照系失效**: 任何以旧数字为基准的新优化、新数学（阈值、锚点、归因）不保证正确 |
| D8 | 判定锚点 EXP-20260817-51（IC +0.0331 / Sharpe −0.886） | 已完成且种子可复现，但 A1-A5 改变训练语义 | 补跑 transformer 前锚点必须重算，阈值 0.0331/0.0482 须重声明 |

### 1.3 受影响数字资产清单（「算完即提交」部分）

| 资产 | 位置 | 受哪些修复影响 |
|---|---|---|
| 78 份 EXP-*.json（2026-08-16 起） | 磁盘 outputs/（gitignore，不入库） | A1/A2（训练口径）、A4（IC 口径）、A5（特征） |
| 登记表全部实验行 | `docs/EXPERIMENT_REGISTRY.md` | 同上 |
| NO-GO 判定证据 4 条（IC 0.037/0.048、300 股 10 倍差距、周线全负） | `docs/ROADMAP.md` | A4（IC 高估方向）、A1/A2（训练） |
| 消融归因（记忆无增益/CDAP 无效/g2-g3 负贡献） | `docs/RESEARCH_FINDINGS_2026-08-17.md` | A3（记忆语义，已加条件性脚注）、A5（特征组消融失真） |
| 判定书 | `docs/DECISION_20260930.md` | 经由 ROADMAP 证据传导 |
| 工作日志 3 份（含已作废旧数字） | `Learn-new/` | K3 B8 已列横幅方案 |
| MLP 锚点 EXP-20260817-51 | 登记表 L112 | A1/A2/A5（训练+特征） |

### 1.4 正确性影响分级（对「后续优化与数学公式搭建」的回答）

| 级别 | 范围 | 结论 |
|---|---|---|
| **稳** | NO-GO 主判定方向 | 基于整体差距（IC 0.02→0.05 量级 + 300 股 10 倍），A 系列口径偏移是同向小幅收紧，不翻转方向；仍须 B4 复核后补确认脚注 |
| **疑** | 全部单项归因、全部阈值/锚点、g2/g3 特征组结论 | 以旧数字搭建的任何新数学（新阈值、新对照、新公式标定）**不可直接引用**，须等 B4 新锚点 |
| **废** | transformer 线的「待跑实验矩阵」原判据 | 训练语义已变（A1 时序切分/A3 记忆对齐/A5 特征去重），原 IC 阈值 0.0331/0.0482 作废待重声明 |

### 1.5 审计结论

工作流程断点 = **transformer 扩规模批次中途暂停**（D1-D5）+ **历史数字未过复核关**（D6-D8）。
后续一切优化与数学搭建的正确参照系 = A1-A5 修复后的新锚点（B4 产出）；
在此之前，B2/B3 先行封存旧数字的引用面（文档标注），防止失真继续传导。

---

## 2. 补丁清单 B2+

| # | 问题 | 位置 | 预设路径 | 依赖/时机 |
|---|---|---|---|---|
| **B2** | 历史数字口径标注缺失——登记表/ROADMAP/RESEARCH_FINDINGS/DECISION 的旧数字未标「旧口径，不可与修复后直接比较」 | 上述 4 文档 | 顶部加统一标注块（仿 Learn-new 作废横幅）；判定书加 A 系列影响脚注 | 无，立即可做 |
| **B3** | 计划文档状态失真——PLAN/UPDATE_LOG 的「恢复步骤」未含 A 系列修复前置；P0-P3 候选未标状态 | PLAN + UPDATE_LOG | PLAN §1 头部加「恢复前置: 先完成 B4 锚点重算」；§2 判据标注「作废待 B4 重声明」；§4 P0-P3 标注挂起 | 无，立即可做 |
| **B4** | 锚点失效——EXP-20260817-51 产出自旧训练语义 | 重跑 `run_full_pipeline_oos.py`（MLP/100 股 128×4 mem-ablate，seed 42） | 同配置重算 → 登记 EXP 新 ID（标注「A1-A5 后锚点」）→ 重声明 transformer 判定阈值 | 需 GPU/真实数据；**时机由维护者定** |
| **B5** | transformer 批次悬空（D1-D3）+ 判定路径未收口（D4） | `scripts/train_transformer.py` 三任务 | 以 B4 新锚点为对照补跑波1/波2 → 按 PLAN §3 路径收口判定（推翻或维持，写入 DECISION） | 依赖 B4 |
| **B6** | （=K3 B1）配置双轨: configs/*.yaml 从未接线 | configs/ vs scripts/ | 单一配置源二选一；未接线文件标注 research-only | 按需 |
| **B7** | （=K3 B2）SiTU 激活三处重复定义 | models/ | 提取共享模块；SELF_CHECK 公式断言同步 | 按需 |
| **B8** | （=K3 B3）特征引擎 Python 级逐时步循环 | features/ | 向量化（unfold 先例） | 扩池前 |
| **B9** | （=K3 B4）regime mask 硬编码 OHLCV 列索引 | expert_trainer 等 | 统一走 ensure_base_panel 或特征名断言 | 按需 |
| **B10** | （=K3 B5）baostock 缓存键不含 frequency | baostock_adapter | frequency 入键 + 安全加载参数 | d/w 切换前必须 |
| **B11** | （=K3 B6）涨跌停阈值近似未处理 ST 5% | baostock_adapter | 对齐文档与实现 | 扩池前 |
| **B12** | （=K3 B7）产物治理: config_hash 不含代码版本/数据指纹 | utils/experiment.py 等 | git commit + 数据缓存 hash 纳入产物 | B4 前建议先做 |
| **B13** | （=K3 B8）文档数字口径漂移（384/390/395/396 并存）+ Learn-new 旧日志无作废横幅 | README/SPEC 等 | 统一以最近 self_check 为准；旧日志加横幅（与 B2 合并执行） | 与 B2 同批 |
| **B14** | （=K3 B9）scripts/ 游离脚本未登记状态 | scripts/ | 盘点分类: 主入口/研究一次性/工具 | 按需 |
| **B15** | （=K3 B10）测试覆盖缺口（weekly/resample 回归等） | tests/ | 按问题清单逐项补 | 按需 |

> A 系列遗留重跑事项（A4 对决同口径重跑、A5 特征组消融复核、A3 预注册内复核）
> 并入 B4 批次执行——一次数据拉取，多问收口。

---

## 3. 行动顺序与完成判据

```
B2 + B3（文档封存，立即） → B12（产物治理，建议先行） → B4（锚点重算，GPU）
  → B5（transformer 收口） → B6-B15（工程债，按需穿插）
```

B 类批次完成判据: ① §1.2 偏差表全部行有归宿（补丁合入或显式挂起）；
② §1.3 资产清单全部完成口径标注；③ 新锚点（B4）落地后，DECISION 判定书
完成收口脚注；④ self_check 5.8 节守卫常绿。

## 4. 挂起项（沿用 K3 C 级决策，B 类不得擅自重启）

- `feat/residual-gate-port` 记忆门收缩先验: 挂起不移植；
- top-3 真稀疏 / AHM 硬化引擎 / Markowitz: 维持 K3/ROADMAP 挂起决策；
- 交易系统投入、加专家数/改路由器/换种子救援: 预注册明令禁止。
