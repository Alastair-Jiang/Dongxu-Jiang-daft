# DAFT 周线验证 · 判据预注册（2026-08-17）

> 预注册性质：本文件在实验运行**之前**锁定全部判据。跑完只按此落闸，
> 不得事后改判据、不得补做「救援」实验。K3 2026-08-17 定夺，DS 执行。

## 一、背景与一句话理由

日频口径下 DAFT 已被证伪（见 `DECISION_20260930.md` 证据链）：
- 方案 A 修复路由塌缩后，full OOS IC +0.0066，Sharpe −2.44（Ridge +0.555）。
- E1/E2 诊断证明：不是「路由摊薄」也不是「专家同质」，而是
  **最强单专家 IC 0.0355 也因换手 2.5×5bp 被成本吃穿**，日频全负。

本轮是**唯一尚未证伪的架构级假设**：换到周频（换手天然降 ~5×，成本占比
大幅下降）后，DAFT 的专家/路由能否转正。这是「便宜的一枪堵漏洞」，不是
因为看好。

K3 先验：🔴 65–70%，🟡→🔴 25–30%，🟢 5–10%。

## 二、冻结表（只改这些，其余一律不动）

| 项 | 设定 |
|---|---|
| 重采样 | W-FRI，现有日线本地生成，**不引新数据源、不重新下载** |
| 特征 | 同一特征集；窗口类 lookback ÷5（20 日→4 周）；每周取最后交易日快照 |
| 切分 | **保持日频相同的日历切分日期**，按周重索引，OOS 完全一致 |
| 回测 | 每周调仓，top20% 多空，成本 5bp+1bp 不变 |
| 模型组 | ① Ridge 周线基线（强制）② DAFT-A ③ DAFT-noKL；种子固定 ×3 |

**DAFT-A** = 方案 A（balance KL, `balance_weight=0.01`，Stage 2 温度 1.0→0.5）。
**DAFT-noKL** = 关闭负载均衡（`balance_weight=0`），其余同 DAFT-A。

## 三、统计口径（关键）

周线 OOS 仅 ~100 点，功效差 5 倍 → 主指标改用：

- **按周配对的 ΔRankIC = DAFT − Ridge**（逐周：DAFT 截面 Rank IC − Ridge 截面 Rank IC）
- 配对 t 检验 + block=8 周 bootstrap 95% CI

次指标（辅助，不作落闸主判据）：净 Sharpe、年化收益、换手。

## 四、落闸判据（预注册，跑前锁定）

| 灯 | 条件 | 行动 |
|---|---|---|
| 🟢 | 配对 ΔIC ≥ −0.005（CI 覆盖 0）**且** 净 Sharpe>0 **且** ≥0.8×Ridge 周线 **且** DAFT 周线 Rank IC ≥ 0.02 | 架构存活（仅限周频），不构成生产 GO |
| 🟡 | Sharpe>0 但 ΔIC<−0.005（执行驱动型转正） | 仅触发一次 3-lite：换手惩罚 λ∈{0,1e-3,1e-2} 或 EMA α∈{0.3,0.5}，Ridge 同步对照，≤1.5 天，无二轮 |
| 🔴 | 最佳变体 Sharpe≤0；**或** ΔIC 点估 <−0.01 且 CI 上界<0；**或** IC 绝对值<0.02 | 立即方向 2，判决书 1 天内定稿 |

> **绝对护栏（2026-08-17 DS 补注，落闸前锁定）**：🟢 的 `DAFT 周线 Rank IC ≥ 0.02`
> 是绝对信号下限，与 🔴 的 `IC<0.02` 对称闭环。它防的是「Ridge 周线本身太弱时，
> `0.8×Ridge` 相对门槛退化成 ≈0，导致『Ridge 烂、DAFT 相对不烂但绝对上也是
> 零信号』被误判为架构存活」。DAFT 周线 IC 不过 0.02，无论 Ridge 多弱都到不了绿灯。

## 五、明令禁止（防僵尸迭代）

1. 不换种子重跑挑结果。
2. 不「查泄漏」行救援。
3. 不加专家数、不改路由器再试。
4. 不做月频（OOS 仅 ~24 点，无功效）。

## 六、时间预算（总计 ≤1 周）

- D0：判据预注册 + 判决书骨架（0.5 天，与 CPU 并行）
- D1–D3：方向 1 周线全流程（2 天 CPU）
- D3 末：落闸（0.5 天）
- D4（条件）：仅黄/绿灯时 3-lite（≤1.5 天）
- D4–D5：判决书定稿（1 天）

最坏 ≤5 天，期望 ≈3.5 天。
